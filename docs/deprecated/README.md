# Deprecated documentation

Documents kept for the record whose figures no longer reproduce against the current setup.
Each carries a banner naming the corpus or test set it was measured against.

**Current retrieval results are in `../../evals/retrieval-stack-findings.md`.**

| file | why it is here |
|---|---|
| `evaluation-results.md` | measured against a `power-grid-model` plus `power-grid-model-ds` corpus |

Deprecating the numbers does not reverse decisions they justified. The BM25-only default
retriever is still the shipped behavior.
